export function JournalTab() {
  return (
    <div className="stub">
      <h2>Journal</h2>
      <p style={{ color: 'var(--muted)' }}>
        Every sim trade lands here with its grade, checklist, and R-multiple — plus the
        trajectory dashboard (grade distribution over time, equity curve, expectancy).
        Arrives with the sim engine.
      </p>
    </div>
  )
}
