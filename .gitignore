# secrets
.env

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# node
node_modules/
dist/

# local databases (default lives in %LOCALAPPDATA%, but never commit one)
*.db
*.db-wal
*.db-shm

# user-data backups (synced by OneDrive, never committed)
backups/
